import styled from "styled-components";

const StyledTransactionsTableContainer = styled.div`
    grid-area: transactionsTable;
    overflow: scroll;
`;

export default StyledTransactionsTableContainer;
