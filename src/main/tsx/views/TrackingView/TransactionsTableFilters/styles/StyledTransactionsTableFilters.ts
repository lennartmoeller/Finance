import styled from "styled-components";

const StyledTransactionsTableFilters = styled.div`
    grid-area: transactionsTableFilters;
    display: grid;
    grid-auto-flow: column;
    justify-content: center;
    gap: 10px;
`;

export default StyledTransactionsTableFilters;
