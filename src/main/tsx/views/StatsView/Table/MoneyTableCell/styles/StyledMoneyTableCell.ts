import styled from "styled-components";

const StyledMoneyTableCell = styled.div`
    display: grid;
    grid-template-columns: max-content max-content;
    gap: 6px;
    align-items: center;
`;

export default StyledMoneyTableCell;
