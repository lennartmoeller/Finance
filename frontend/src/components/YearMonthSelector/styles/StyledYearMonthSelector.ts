import styled from "styled-components";

const StyledYearMonthSelector = styled.div`
    display: grid;
    grid-template-columns: max-content 100px max-content;
    align-items: center;
`;

export default StyledYearMonthSelector;
