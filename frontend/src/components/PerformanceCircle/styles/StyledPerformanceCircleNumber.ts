import styled from "styled-components";

const StyledPerformanceCircleNumber = styled.div`
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 16px;
    font-weight: 600;
    letter-spacing: -1px;
`;

export default StyledPerformanceCircleNumber;
